FROM ubuntu

RUN apt-get update && apt-get install -y git python3 python3-pip gcc locales-all gcc-multilib

RUN pip3 install /test

ENV LC_ALL en_US.UTF-8
ENV LC_LANG en_US.UTF-8

# just some simple tests
CMD syscall_number -s read -b 32; syscall_number -a -b 64